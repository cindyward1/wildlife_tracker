<h1><b>Wildlife Tracker</b>&nbsp&nbsp&nbsp&nbsp<%= image_tag('wildlife_tracker.png') %></h1>

<h3>Update the following information about the species:</h3>
<form action="/species/<%= @specie.id %>" method="post">
  <input name="_method" type="hidden" value="patch">
  <label for "species_name">Species Name &nbsp&nbsp&nbsp</label>
  <input id="species_name" name="name" type="text" value="<%= @specie.name %>">
  <br><br><button style="background-color:yellow">Update Species Name</button>
</form><br>
  <button style="background-color:red"><a href="/species/<%= @specie.id %>"
        data-confirm="Are you sure you want to delete this species including ALL of its sightings?"
        data-method="delete" rel="nofollow">Delete Species</a>
        </button>&nbsp&nbsp<b>This will delete ALL sightings for this species!</b><br>
<h3>Sightings For This Species</h3>
<ul>  <% @sightings.each do |sighting| %>
        <% if sighting.subspecies == "" %>
          <% subspecies_str = "None" %>
        <% else %>
          <% subspecies_str = sighting.subspecies %>
        <% end %>
  <li><%= sighting.date %>, Latitude = <%= sighting.latitude %>, Longitude = <%= sighting.longitude %>,
                            Subspecies = <%= subspecies_str %>
  </li>
      <% end %>
</ul>
<br>
<h4><a href="/sightings">Record a sighting</a></h4>
<h4><a href="/sightings/all">See and maintain the list of sightings</a></h4>
<h4><a href="/species/new">Add a new species</a></h4>
<h4><a href="/species">See and maintain the list of species that have been sighted</a></h4>
